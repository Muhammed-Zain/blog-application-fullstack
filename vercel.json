{
     "buildCommand": "(cd client) -and (npm install) -and (npm run build)",
     "outputDirectory": "client/dist",
     "rewrites": [
          {
               "source": "/api/(.*)",
               "destination": "/api/index.js"
          }
     ]
}